<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Email Template</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: Arial, sans-serif;
      background-color: #f9f9f9;
    }

    table {
      width: 100%;
      height: 100vh;
    }

    .container {
      display: table-cell;
      text-align: center;
      vertical-align: top;
      background: pink;
      border-radius: 5px;
      border: 1px solid pink;
      padding: 20px;
    }

    .content {
      width: 550px;
      margin: 0 auto;
      overflow: hidden;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
    }

    .content .card {
      background-color: #fff;
      border-radius: 5px;
      padding: 20px;
      margin-bottom: 20px;

      border-bottom: 1px solid #f9f9f9;
      padding-bottom: 20px;
    }


    .message-content {
      font-size: 1.5rem;
      font-weight: 400;
      color: #353535;
    }



    .footer {
      background-color: pink;
      filter: brightness(0.9);
      border-top: 1px solid #f9f9f9;
      padding-top: 10px;
    }

    .button {
      background-color: rgb(250, 206, 213);
      border: 1px solid rgb(255, 142, 161);
      padding: 10px;
      border-radius: 5px;
      margin: 10px;
      cursor: pointer;
      text-decoration: none;
    }
  </style>
</head>

<body>
  <table>
    <tr>
      <td class="container">
        <div class="content">
          <div>
             {{#each jobs}}
            <div class="card">
                <h2>{{title}}</h2>
                <p>{{description}}</p>
                <p>Local: {{location}}</p>
                <a class="button" href="https://www.infojobs.com.br/{{link}}">Detalhes da vaga</a>
            </div>
            {{/each}}
          </div>

          <div class="footer">
            <p>
              Bot rodou às {{date}}.
            </p>
          </div>
        </div>
      </td>
    </tr>
  </table>
</body>

</html>
